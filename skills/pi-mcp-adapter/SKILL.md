---
name: pi-mcp-adapter
description: Bridge selected MCP servers through Pi with a compact tool interface instead of loading all MCP tools at session start.
disable-model-invocation: true
---

# Pi MCP Adapter

Use when you need external MCP tools as a controlled Pi tool layer.

- `/mcp` manages servers and status.
- `mcp_tool()` executes mapped MCP tool calls via adapter flow.

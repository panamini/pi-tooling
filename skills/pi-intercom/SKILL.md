---
name: pi-intercom
description: Send and receive coordination messages across Pi sessions for parallel work, asks, and handoffs.
disable-model-invocation: true
---

# Pi Intercom

Use for cross-session coordination.

- Run `/intercom` to open the session messaging overlay.
- Use `intercom()` for scripted sends/asks/replies.
- Useful to bridge planner, worker, and reviewer sessions.

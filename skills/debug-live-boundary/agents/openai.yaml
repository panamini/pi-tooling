interface:
  display_name: "Debug Live Boundary"
  short_description: "Trace live behavior to the real boundary"
  default_prompt: "Use $debug-live-boundary to debug behavior that fails live but passes locally."
